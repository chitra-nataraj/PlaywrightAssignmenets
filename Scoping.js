const browserVersion="chrome"; //global const
function getBrowserVersion(){
    if(browserVersion=="chrome")
    {
        //var localVar="I am a local variable inside a block";
        let  localVar="converting var into let";
    }
    console.log(localVar);
    
}
getBrowserVersion();